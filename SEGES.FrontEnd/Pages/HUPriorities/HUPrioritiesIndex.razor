@page "/HUPriorities"
<h3>Prioridades de Historias de Usuario</h3>

<div class="mb-3">
    <a class="btn btn-primary" href="/HUPriorities/create">Nueva Prioridad</a>
</div>

<GenericList2 MyList="huPriorities">
    <Loading>
        <div class="d-flex justify-content-center align-itmes-center">
            <img src="https://i.gifer.com/ZKZg.gif" alt="Cargando..." />
        </div>
    </Loading>
    <Body>
        <Pagination CurrentPage="currentPage"
                    TotalPages="totalPages"
                    SelectedPage="SelectedPageAsync" />
        <table class="table table-striped">
            <thead>
                <tr>
                    <th>País</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var priority in huPriorities!)
                {
                    <tr>

                        <td>@priority.Name</td>
                        <td>
                            <a class="btn btn-info btn-sm" href="/HUPriorities/edit/@priority.PriorityId">Editar</a>
                            <button @onclick=@( () => DeleteAsync(priority) ) class="btn btn-sm btn-danger">Borrar</button>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </Body>
</GenericList2>
