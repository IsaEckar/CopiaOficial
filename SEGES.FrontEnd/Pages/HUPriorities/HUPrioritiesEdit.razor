@page "/HUPriorities/edit/{PriorityId:int}"

<h3>Editar Prioridad de Historias de Usuario</h3>

@if (priority is null)
{
    <Loading />
}
else
{
    <FormWithName @ref="priorityForm" Label="Prioridad:" Model="priority" OnValidSubmit="SaveAsync" ReturnAction="Return" />
}
