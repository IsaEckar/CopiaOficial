@page "/HUApprovalStatus"
<h3>Estados de aprobación de Historias de Usuario</h3>

<div class="mb-3">
    <a class="btn btn-primary" href="/HUApprovalStatus/create">Nuevo Estado de Aprobación</a>
</div>
<Filter PlaceHolder="Buscar..." Callback=@FilterCallBack />
<GenericList2 MyList="hUApprovalStatuses">
    <Loading>
        <div class="d-flex justify-content-center align-itmes-center">
            <img src="https://i.gifer.com/ZKZg.gif" alt="Cargando..." />
        </div>
    </Loading>
    <Body>
        <Pagination CurrentPage="currentPage"
                    TotalPages="totalPages"
                    SelectedPage="SelectedPageAsync"
                    RecordsNumber="SelectedRecordsNumberAsync" />

        <table class="table table-striped">
            <thead>
                <tr>
                    <th>Estado</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var status in hUApprovalStatuses!)
                {
                    <tr>

                        <td>@status.Name</td>
                        <td>
                            <a class="btn btn-info btn-sm" href="/HUApprovalStatus/edit/@status.HUApprovalStatusId">Editar</a>
                            <button @onclick=@( () => DeleteAsync(status) ) class="btn btn-sm btn-danger">Borrar</button>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </Body>
</GenericList2>
